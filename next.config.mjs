/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["fakeimg.pl", "static.wixstatic.com"],
        formats: ["image/webp"]
    }
};

export default nextConfig;
